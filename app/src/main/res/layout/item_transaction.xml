<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="6dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeWidth="1dp"
    app:strokeColor="?attr/colorOutline"
    app:cardBackgroundColor="?attr/colorSurfaceVariant">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Icon circle — letter initial of bank name -->
        <TextView
            android:id="@+id/tvIcon"
            android:layout_width="42dp"
            android:layout_height="42dp"
            android:background="@drawable/circle_bg"
            android:gravity="center"
            android:textSize="16sp"
            android:fontFamily="@font/nunito_bold"
            android:textColor="?attr/colorOnPrimaryContainer"
            tools:text="H"/>

        <!-- Middle: name, merchant, date, category chip -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvSender"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="?attr/colorOnSurface"
                android:textSize="14sp"
                android:fontFamily="@font/nunito_bold"
                tools:text="HDFC Bank"/>

            <!-- Hidden when no merchant extracted -->
            <TextView
                android:id="@+id/tvMerchant"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="12sp"
                android:visibility="gone"
                tools:visibility="visible"
                tools:text="Swiggy"/>

            <TextView
                android:id="@+id/tvDate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:textSize="11sp"
                android:alpha="0.7"
                tools:text="21 Mar, 09:01 PM"/>

            <!-- Category chip -->
            <TextView
                android:id="@+id/tvCategory"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="6dp"
                android:background="@drawable/chip_bg"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingTop="3dp"
                android:paddingBottom="3dp"
                android:textSize="10sp"
                android:fontFamily="@font/nunito_bold"
                android:textColor="?attr/colorOnSecondaryContainer"
                tools:text="Food"/>

        </LinearLayout>

        <!--
            Amount — textColor is set in code via MaterialColors.getColor()
            so it reads colorError (debit) / colorSecondary (credit) from theme.
            android:textColor here is the fallback shown in layout editor only.
            tools:textColor shows a debit-style red in Android Studio preview.
        -->
        <TextView
            android:id="@+id/tvAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            android:fontFamily="@font/nunito_bold"
            android:layout_marginStart="10dp"
            android:textColor="?attr/colorOnSurface"
            tools:text="-₹450"
            tools:textColor="?attr/colorError"/>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
